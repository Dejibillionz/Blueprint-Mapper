import * as THREE from "three";

export class DiscordPortal {
  constructor(_scene: THREE.Scene, _x: number, _y: number, _z: number) {
  }

  update(_delta: number): void {
  }

  dispose(): void {
  }
}
